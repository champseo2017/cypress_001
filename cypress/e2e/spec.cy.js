// ไฟล์ทดสอบตัวอย่างสำหรับ E2E
describe('ตัวอย่างการทดสอบ', () => {
  it('เข้าเว็บไซต์หลักได้', () => {
    // เข้าเว็บไซต์
    cy.visit('http://localhost:5173')
    
    // ถ่ายภาพหน้าจอแบบปกติ
    cy.screenshot('หน้าแรก')
    
    // ถ่ายภาพเฉพาะส่วน (ต้องระบุ selector ของส่วนที่ต้องการ)
    // cy.get('.header').screenshot('ส่วนหัวเว็บไซต์')
    
    // ถ่ายภาพแบบเต็มหน้าจอ (รวมส่วนที่ต้อง scroll)
    cy.screenshot('หน้าแรก-เต็มหน้าจอ', {
      capture: 'fullPage'
    })
  })
}) 